{
  "name": "bastak-leads-gate",
  "version": "1.0.0",
  "private": true,
  "description": "Invite-only auth gate in front of the Bastak Market Intelligence dashboard. Deployed to Vercel; the actual dashboard.html (with real lead data) lives in a private Supabase Storage bucket and is only ever streamed back by api/dashboard.js after a verified, allow-listed session.",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cookie": "^0.7.1"
  }
}
