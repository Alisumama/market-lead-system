#!/bin/sh
# Auto-increment the BUILD number (the +N in `version:`) on every commit.
pubspec="pubspec.yaml"
[ -f "$pubspec" ] || exit 0
perl -i -pe 's/^(version:\s*\d+\.\d+\.\d+\+)(\d+)/$1 . ($2 + 1)/e' "$pubspec"
git add "$pubspec"
exit 0
